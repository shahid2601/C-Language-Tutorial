#include <stdio.h>
int main()
{
    int a;
    char ch = 'a';
    ch = '\n'; // represents a new line
    ch = '\t'; // represents a tab space
    ch = '\\'; // represents a backslash
    ch = '\b'; // represents a backspace
    ch = '\''; // represents a single quote
    ch = '\"'; // represents a double quote

    printf("Hey, myself\n shahid.");

    return 0;
}