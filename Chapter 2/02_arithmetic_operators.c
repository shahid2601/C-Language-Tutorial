#include <stdio.h>

int main()
{

    int a = 4, b = 3, c = a + b;

    printf("The value of a is %d and b is %d and sum is %d\n", a, b, c);
    // The Modulus Operator is used to get remainder.
    printf("The remainder when a is divided b is: %d", a%b);

    return 0;
}